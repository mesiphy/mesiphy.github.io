<section id="projects" class="projects" aria-labelledby="projects-title">
  <h2 id="projects-title" class="section-title">项目实践</h2>
  <section class="project-row" aria-labelledby="effipanel-title">
    <div>
      <p class="project-meta">2026 · Windows 桌面应用 · 优化版预览</p>
      <h3 id="effipanel-title"><a href="/projects/effipanel/">逸扉面板 · EffiPanel</a></h3>
      <p>把键位启动、统一搜索、剪贴历史、截图标注和草稿，集中到一个随时可唤起的效率面板。</p>
      <p class="project-role">功能图解 · 使用场景 · 首次上手指南</p>
    </div>
    <a class="project-link" href="/projects/effipanel/" aria-label="阅读逸扉面板 EffiPanel 产品介绍">了解产品 →</a>
  </section>

  <section class="project-row" aria-labelledby="learnbranch-title">
    <div>
      <p class="project-meta">2026 · Windows 桌面应用 · AI 产品工程</p>
      <h3 id="learnbranch-title"><a href="/projects/learnbranch/">LearnBranch</a></h3>
      <p>把线性的 AI 对话，变成可追溯、可审核、可整理的学习树。</p>
      <p class="project-role">产品定义 · AI 协作编排 · 代码阅读与人工验收</p>
    </div>
    <a class="project-link" href="/projects/learnbranch/" aria-label="阅读 LearnBranch 完整案例">阅读案例 →</a>
  </section>
</section>

<style>
  .projects { scroll-margin-top: 2rem; margin-block: 2rem 2.75rem; }
  .projects > h2 { margin: 0; }
  .project-row {
    display: grid;
    gap: 1.4rem;
    padding-block: 2rem;
    border-bottom: 1px dashed var(--rule);
  }

  @media (min-width: 40rem) {
    .project-row {
      grid-template-columns: 1fr auto;
      align-items: end;
    }
  }

  .project-meta,
  .project-role {
    margin: 0 0 0.55rem;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--ink-faint);
  }

  .project-row h3 {
    margin: 0 0 0.55rem;
    font-size: 1.8rem;
  }

  .project-row h3 a,
  .project-link {
    text-decoration: none;
  }

  .project-row > div > p:not(.project-meta, .project-role) {
    max-width: var(--measure);
    margin-bottom: 0.65rem;
    color: var(--ink-soft);
  }

  .project-link {
    font-family: var(--font-ui);
    font-size: 0.85rem;
    color: var(--accent);
  }
</style>
